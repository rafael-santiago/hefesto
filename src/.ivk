#
# Hefesto's invoke file
#
# It allows you to invoke the forge just typing "hefesto".
#

--forgefiles=Forgefile.hsl --Forgefile-projects=hefesto --compile-model=debug --link-model=shared --ldflags=-rdynamic
