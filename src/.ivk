#
# Hefesto's invoke file
#
# It allows you to invoke the forge just typing "hefesto".
#

--forgefiles=Forgefile.hsl --Forgefile-projects=hefesto --compile-model=release --link-model=shared --ldflags=-rdynamic --cflags=-DHVM_ASYNC_RQUEUE --includes=here/src --libraries=here/src/lib
