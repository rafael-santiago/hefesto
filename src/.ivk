#
# Hefesto's invoke file
#
# It allows you to invoke the forge just typing "hefesto".
#

--forgefiles=Forgefile.hls --Forgefile-projects=hefesto --compile-model=release --link-model=shared --ldflags=-rdynamic
